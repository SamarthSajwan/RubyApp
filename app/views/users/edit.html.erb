Edit Data
<hr>

  <%= form_for :user, url : user_path(@user), method: :patch do |f| %><br><br>  
	Name  <%= f.text_field :name %><br><br>
	Email <%= f.text_field :email %><br><br>
	Phone <%= f.text_field :phone %><br><br>	
	<%= f.submit({:class => 'btn btn-primary'}) %>
<% end -%>

<hr>

<!-- All UsersInfo<br><br>

<%if !@users.blank? %>
	<% for u in @users %>
		<%= u.name%> <%= u.email%> <%= u.phone%> <br>

	<% end %>

<% else %>

<% end %> -->